__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
data/
results/
*.egg-info/

# VS Code / code-server state written inside the capsule. These are IDE logs, caches
# and session keys, not source: they churn on every session and code/.vscode/
# serve-web-key-half is a random web-server key that has no business in a public repo.
code/.vscode/
.vscode/
